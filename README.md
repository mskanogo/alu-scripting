# Regular Expressions

This project contains Ruby scripts that use regular expressions (Oniguruma)
to match specific patterns as part of the ALU Front-End Web Development
curriculum.

Each script accepts one argument and prints the result of applying
a regular expression using Ruby.
